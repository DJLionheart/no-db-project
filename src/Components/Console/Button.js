import React from 'react';
import { Button } from 'reactbulma';

const Button = (props) => {
    return(
        <button>
            Add to playlist
        </button>
    )


}


export default Button;